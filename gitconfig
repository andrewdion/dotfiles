[user]
    name = Andrew Dion
    email = andrew_dion@hms.harvard.edu

[push]
    default = simple

[color]
    ui = true
    diff = auto

[alias]
    st = status
    br = branch
    co = checkout
    di = diff
    ci = commit
    log = log --color
    unstage = reset HEAD --
    cleanup = !git-cleanup $1
    # this is close to working...
    #cleanup = "!f() { git branch --merged | egrep -v '^(\*|  master|  development)' | xargs -r git branch -d; }; f"
    #cleanup = "!f() { git branch --merged | egrep -v '^(\*|  master|  development)' | xargs -r git branch -d && git remote prune origin; }; f"
