#!/bin/bash

if [ $# -eq 0 ]; then
    cat /etc/hosts
    #fgrep endor /etc/hosts
    exit 0
fi

ip=$1

cp /etc/hosts /etc/hosts.bak

awk -F " " -v ip=$ip '$3 == "endor" {$0 = "10.119.216."ip"  endor.localdomain       endor"} {print}' /etc/hosts.bak > /etc/hosts

cat /etc/hosts
